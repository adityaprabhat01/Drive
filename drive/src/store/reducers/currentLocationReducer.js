const initState = {}

const currentLocationReducer = (state = initState, action) => {
  switch (action.type){
    case 'CURRENT_LOCATION':
      return state
  }
}

export default currentLocationReducer